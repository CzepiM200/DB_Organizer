import React, { FunctionComponent } from "react";

type HomePageProps = {};

export const HomePage: FunctionComponent<HomePageProps> = (props: any) => {
  return <section>Strona powitalna</section>;
};

const usersMainData = (state = [], action: any) => {
  switch (action.type) {
    case "LOAD":
      return state;
      break;

    default:
      return state;
      break;
  }
};

export default usersMainData;
